{
  "name": "bukumenu_digitalpsr_root",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:frontend": "cd pawon-salam-digital-menu/frontend && npm install && npm run build",
    "build:backend": "cd pawon-salam-digital-menu/backend && npm install",
    "build": "npm run build:frontend"
  }
}
