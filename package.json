{
  "name": "bukumenu_digitalpsr_root",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:android": "cd frontend && npm install && npm run build:android",
    "build:backend": "cd backend && npm install",
    "build": "npm run build:frontend",
    "guardrail:deploy": "node scripts/deploy-guardrail.js"
  }
}
